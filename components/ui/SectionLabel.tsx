export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <span className="text-[11px] uppercase tracking-[0.22em] font-semibold text-burnt">
      {children}
    </span>
  )
}
